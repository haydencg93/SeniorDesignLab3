# SeniorDesignLab3
 install supabase:
 ``` linux
 npm install supabase --save-dev
 ```

make it a supabase
``` linux
npx supabase init
```

 add verify-password:
 ``` linux
 supabase functions new verify-password
 ```

 login